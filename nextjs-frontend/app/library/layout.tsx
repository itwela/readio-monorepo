import LibraryClient from "./page";

export default async function LibraryServer() {
    // try {
        return (
            <>
                <LibraryClient />
            </>
        );
    // } catch (error) {
        // return <div className="w-full h-screen flex place-content-center place-items-center">Something went wrong</div>;
    // }
}